// Retorna um booleano verificando se determinado elemento existe no array
const item = [1,3,3,4,3];

const hasItemOne = item.includes(1);
// true 

const hasItemOne = item.includes(2);
// false
